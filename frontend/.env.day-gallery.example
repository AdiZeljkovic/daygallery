# ======================================================================
# frontend/.env.production  —  PRODUKCIJA: day-gallery.com
# Kopiraj u frontend/.env.production PRIJE `npm run build --workspace frontend`.
#
# ⚠️ NEXT_PUBLIC_* se "peče" u bundle pri BUILDU.
# Ako ovo promijeniš a ne uradiš rebuild → promjena NEMA efekta (pm2 restart nije dovoljan).
# ======================================================================

# Ista domena — nginx rutira /api, /uploads i /socket.io na backend (port 4712)
NEXT_PUBLIC_API_URL="https://day-gallery.com"
