# ======================================================================
# PRODUKCIJA — frontend/.env.production na VPS-u
# Kopiraj u frontend/.env.production PRIJE `npm run build`.
# NEXT_PUBLIC_* varijable se "peku" u build, zato moraju postojati prije builda.
# ======================================================================

# Javni API — ista domena (nginx rutira /api i /socket.io na backend)
NEXT_PUBLIC_API_URL="https://daygallery.adizeljkovic.com"
